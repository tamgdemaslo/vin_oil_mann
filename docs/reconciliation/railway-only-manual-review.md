# Railway-only manual review

Открытых записей: **10**. Значения PII не выводятся.

- **communication_identities / …f02c21601956** (CommunicationIdentity). Причина: Durable identity/link has no unambiguous Selectel business-key match and uses logical relations not enforced by database foreign keys. Связи: 0 parent / 0 child. При импорте возможна новая связь или объект; при пропуске может потеряться историческая привязка. Рекомендация: **MAP_TO_EXISTING после подтверждения владельцем**.
- **conversation_entity_links / …288415727047** (ConversationEntityLink). Причина: Durable identity/link has no unambiguous Selectel business-key match and uses logical relations not enforced by database foreign keys. Связи: 0 parent / 0 child. При импорте возможна новая связь или объект; при пропуске может потеряться историческая привязка. Рекомендация: **SKIP_DUPLICATE, если подтверждена эквивалентная связь; иначе MAP_TO_EXISTING**.
- **messenger_attachments / …10b766630f0d** (MessengerAttachment). Причина: Attachment metadata is ready, but file/blob existence is not proven by PostgreSQL dumps; import requires legacy file evidence. Связи: 1 parent / 3 child. При импорте возможна новая связь или объект; при пропуске может потеряться историческая привязка. Рекомендация: **INSERT_MISSING только после подтверждения наличия локального объекта; иначе REJECT_INVALID**.
- **messenger_attachments / …f108829d4856** (MessengerAttachment). Причина: Attachment metadata is ready, but file/blob existence is not proven by PostgreSQL dumps; import requires legacy file evidence. Связи: 1 parent / 2 child. При импорте возможна новая связь или объект; при пропуске может потеряться историческая привязка. Рекомендация: **INSERT_MISSING только после подтверждения наличия локального объекта; иначе REJECT_INVALID**.
- **messenger_attachments / …992a0f0548b3** (MessengerAttachment). Причина: Attachment metadata is ready, but file/blob existence is not proven by PostgreSQL dumps; import requires legacy file evidence. Связи: 1 parent / 4 child. При импорте возможна новая связь или объект; при пропуске может потеряться историческая привязка. Рекомендация: **INSERT_MISSING только после подтверждения наличия локального объекта; иначе REJECT_INVALID**.
- **messenger_attachments / …d923bf77018f** (MessengerAttachment). Причина: Attachment metadata is ready, but file/blob existence is not proven by PostgreSQL dumps; import requires legacy file evidence. Связи: 1 parent / 3 child. При импорте возможна новая связь или объект; при пропуске может потеряться историческая привязка. Рекомендация: **INSERT_MISSING только после подтверждения наличия локального объекта; иначе REJECT_INVALID**.
- **messenger_attachments / …e4ad12597b2b** (MessengerAttachment). Причина: Attachment metadata is ready, but file/blob existence is not proven by PostgreSQL dumps; import requires legacy file evidence. Связи: 1 parent / 3 child. При импорте возможна новая связь или объект; при пропуске может потеряться историческая привязка. Рекомендация: **INSERT_MISSING только после подтверждения наличия локального объекта; иначе REJECT_INVALID**.
- **messenger_attachments / …71e3a19c39ad** (MessengerAttachment). Причина: Attachment metadata is ready, but file/blob existence is not proven by PostgreSQL dumps; import requires legacy file evidence. Связи: 1 parent / 3 child. При импорте возможна новая связь или объект; при пропуске может потеряться историческая привязка. Рекомендация: **INSERT_MISSING только после подтверждения наличия локального объекта; иначе REJECT_INVALID**.
- **messenger_attachments / …00f7e3769837** (MessengerAttachment). Причина: Attachment metadata is ready, but file/blob existence is not proven by PostgreSQL dumps; import requires legacy file evidence. Связи: 1 parent / 3 child. При импорте возможна новая связь или объект; при пропуске может потеряться историческая привязка. Рекомендация: **INSERT_MISSING только после подтверждения наличия локального объекта; иначе REJECT_INVALID**.
- **messenger_connections / …224e1e2c45c5** (MessengerConnection). Причина: Durable identity/link has no unambiguous Selectel business-key match and uses logical relations not enforced by database foreign keys. Связи: 0 parent / 1 child. При импорте возможна новая связь или объект; при пропуске может потеряться историческая привязка. Рекомендация: **MAP_TO_EXISTING после подтверждения владельцем**.
