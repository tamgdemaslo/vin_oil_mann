# MANN unified technical staging: Timeweb apply, 2026-09-04

## Result

The primary-source-verified subset was imported into the Timeweb production
database `vin_oil` as staging-only data.

- canonical vehicles: `4`;
- technical association revisions: `5`;
- review decisions: `5`;
- materialization run: `mtmr_3d17845c128d42fb3099b244`;
- run status/mode: `COMPLETED / STAGING`;
- revision state: `STAGED`;
- verification status: `PRIMARY_SOURCE_VERIFIED_FIELDS`;
- review actor/decision: `SYSTEM_IMPORT / CONFIRM`;
- `apply_eligible=false` for every revision;
- `independent_human_signoff=false`;
- `production_apply_authorized=false`;
- active revisions: `0`.

This is not a runtime cutover. Existing catalog reads remain unchanged.

## Authorization and scope

The owner explicitly authorized Codex to proceed with the update and testing in
the chat on 2026-09-04. The recorded authorization reference is
`owner-chat-2026-09-04-staging-only` and its database scope is
`STAGING_IMPORT_ONLY`.

The operation does not represent an independent specialist signature. The five
decisions use `actor_type=SYSTEM_IMPORT` and explicitly state that independent
technical signoff is absent. The 27 unresolved rows and 33 audited rejections
were not imported.

## Evidence and hashes

- verified staging plan:
  `outputs/mann-unified-technical-staging-v1/mann-unified-technical-staging-plan-v1.json`;
- staging plan SHA-256:
  `a852866c37c2cc0a252895dd10b0fe43ec2cd8704cd642648622066a124ec341`;
- generated apply SQL:
  `outputs/mann-unified-technical-staging-v1/mann-unified-technical-staging-apply-v1.sql`;
- generated SQL SHA-256:
  `f763fee828b86e7f42129755ca052b0ee3353ec4f65662b6d482a4ee0bee6144`;
- source backup SHA-256:
  `44d907b23ea2a30ec20a1a41147f7ba0bc437c3815eaeff18a6026e4bddaa679`;
- required migration:
  `20260902400000_mann_unified_technical_catalog_expand`.

The SQL was generated by
`scripts/build-mann-unified-technical-staging-sql.mjs`. It is transactional,
uses an advisory lock, validates the target database and migration, refuses
canonical hash conflicts, and verifies the final row states before commit.

## Production verification

Preflight before import:

- required migration records: `1`;
- existing canonical vehicles/runs/revisions/decisions: `0 / 0 / 0 / 0`;
- `mann_filter_applications`: `37,600`;
- `vehicle_fluid_requirements`: `13,296`;
- `fluid_source_rows`: `13,287`.

Post-apply verification:

- canonical vehicles/revisions/decisions: `4 / 5 / 5`;
- orphan revisions: `0`;
- decisions without a revision: `0`;
- revisions with missing evidence, wrong verification status, or forbidden
  unverified fields: `0`;
- active or apply-eligible revisions: `0`;
- legacy counts remained `37,600 / 13,296 / 13,287`.

The same SQL was applied a second time as an idempotency test. Counts and state
remained `4 / 5 / 5`, with all five revisions still `STAGED` and
`apply_eligible=false`.

Application readiness after the operation:

- `GET https://www.tamgdemaslocrm.ru/api/health/ready` returned HTTP `200`;
- overall status: `ready`;
- config/database/migrations/appData checks: `ok`.

## Local verification

- `npm run check:timeweb-only` — PASS;
- `npm run test:mann-unified-technical-expand` — PASS;
- `npm run test:mann-unified-technical-staging-sql` — PASS.

## Next gate

Testing may now query the five staged capacities directly or through a
staging-only read path. Activating revisions or switching runtime reads remains
blocked and requires a separate, explicit change with its own tests and
rollback plan.
