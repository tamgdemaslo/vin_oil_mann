# Итог аудита объединения MANN и каталога технических жидкостей — v9

Дата: 2026-09-02  
Режим: только локальное восстановление и dry-run, без записи в production  
Источник: `backup_20260823_190344.sql`  
SHA-256: `44d907b23ea2a30ec20a1a41147f7ba0bc437c3815eaeff18a6026e4bddaa679`

## Короткий вывод

Алгоритмическая часть объединения усилена и повторно проверена на полном бэкапе. Все 33 связи, отклонённые при разборе спорной выборки v6, исключены из автоматического результата: 32 больше не создаются matcher v8, ещё 1 блокируется denylist.

Production не изменялся. Решение о миграции остаётся `NO_GO`, потому что автоматическая проверка структуры не заменяет независимую подпись технического специалиста и полную проверку фактических объёмов по первичным документам производителей.

## Что было исправлено

- matcher больше не отбрасывает сведения о двигателе для тормозной жидкости;
- для `BRAKE_FLUID`, `AC_REFRIGERANT` и `FUEL_TANK` точный код двигателя обязателен, если исходная строка сама содержит двигатель;
- записи без двигателя сохраняют возможность безопасного сопоставления на уровне кузова;
- слишком широкие группы двигателей переводятся в ручную проверку;
- добавлен версионированный denylist из 33 проверенных fingerprint;
- denylist валидируется и применяется до формирования списка materialization;
- отклонённые denylist-связи выводятся в отдельный отчёт и не попадают в ACTIVE/REVIEW-кандидаты;
- проверка итогового preview подтверждает отсутствие denylist fingerprint среди предлагаемых связей;
- отчёт ручной выборки больше не требует точного двигателя или кузова, если такого признака не было в исходных данных.

Код зафиксирован коммитами:

- `f73eb04` — matcher, denylist, dry-run enforcement и тесты;
- `d93a51e` — точный двигатель для сопоставления топливного бака.

## Полный dry-run v9

Каталог результатов: `outputs/mann-technical-catalog-v9-timeweb-backup-20260823-190344/`

| Показатель | v6 | v9 |
|---|---:|---:|
| Требований | 13 296 | 13 296 |
| Строк MANN | 37 600 | 37 600 |
| Вариантов MANN | 16 349 | 16 349 |
| Предложенных связей после дедупликации и policy | 779 | 953 |
| ACTIVE | 715 | 889 |
| REVIEW из-за parser | 64 | 64 |
| Отклонено denylist в этом прогоне | 0 | 1 |
| Покрыто вариантов автомобилей | 389 | 379 |

Рост ACTIVE вызван не ослаблением правил, а тем, что matcher теперь использует точный двигатель для тормозной жидкости и может выбрать корректную модификацию вместо прежнего неразличимого набора кандидатов. Одновременно 32 известных ошибочных fingerprint перестали создаваться matcher.

## Проверки v9

- все 13 296 решений присутствуют в trace;
- все fingerprint итоговых связей уникальны;
- все ACTIVE-цели независимо перевалидированы алгоритмом, без hard conflict и review blocker;
- ни одна parser-review запись не находится в ACTIVE;
- запрещённые fingerprint отсутствуют среди предлагаемых связей;
- 200 ACTIVE и 200 dangerous-system строк покрыты выборкой; из-за пересечения получено 247 уникальных связей;
- 247 из 247 не имеют критических структурных флагов в scope Codex review;
- 30-кейсовый технический truth set: 0 false positive, 3 консервативных false negative;
- 200-кейсовый golden set parser пройден;
- ни один токен мощности в л.с. не принят за литры;
- исходная БД читалась в read-only транзакциях;
- скрипт dry-run по-прежнему запрещает флаги записи в БД.

Основные машинные отчёты:

- `mann-technical-materialization-summary.json`;
- `preview-invariant-check.json`;
- `mann-fluid-review-truth-score.json`;
- `mann-active-codex-review-check.json`;
- `mann-technical-policy-rejections.json`;
- `capacity-parser-audit.json`.

## Разбор 65 спорных связей v6

Подготовлен отдельный Excel `outputs/mann-technical-review-65-20260902/mann-technical-review-65.xlsx`:

- 5 строк подтверждены первичными документами HAVAL и Opel, но требуют независимой подписи;
- 33 текущие связи отклонены;
- 27 строк оставлены на проверке из-за отсутствия достаточного первичного подтверждения.

После matcher v8 этот список используется как аудит происхождения решений, а не как список текущих ACTIVE-кандидатов.

## Что остаётся до миграции

1. Ответственный технический специалист должен независимо подписать подтверждённые значения и выборку ACTIVE.
2. Для фактических объёмов и спецификаций без первичного документа нужно получить руководство производителя или сервисную документацию.
3. После подписи следует создать отдельный apply-план с транзакцией, журналом происхождения и rollback; текущий dry-run-скрипт намеренно не умеет писать в БД.

До выполнения этих пунктов production и runtime cutover не менять.

## Очистка

Временный локальный PostgreSQL и APFS-образ удалены после проверки. Исходный SQL-бэкап сохранён без изменений.
